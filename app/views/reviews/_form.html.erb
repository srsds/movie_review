<%= form_for([@movie,@review]) do |form| %>
  <% if review.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(review.errors.count, "error") %> prohibited this review from being saved:</h2>

      <ul>
      <% review.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  
  <!-- <div class="field">
    <div id="star-rating"></div>
  </div> -->
  <div class="form-group row">
    <%= form.label :rating,class: 'col-sm-2 form-control-label' %>
      <div class="col-sm-6">
    <%= form.number_field :rating, id: :review_rating, class: 'form-control' %>
</div>
  </div>

  <div class="form-group row">
    <%= form.label :comment, class: 'col-sm-2 form-control-label' %>
  <div class="col-sm-6">
      <%= form.text_area :comment, class: 'form-control' %>
  <div>
  </div>
  <br>
  <!-- <div class="field">
    <%= form.label :comment %>
    <%= form.text_area :comment, id: :review_comment %>
  </div> -->
<div class="form-group row">
    <div class="col-sm-10">
      <%= form.submit 'Submit', class: 'btn btn-primary' %>
    <%= link_to "Back", movies_path, class: "btn btn-secondary" %>
    </div>
  </div>

<!-- 
  <div class="actions">
    <%= form.submit %>
  </div> -->
<% end %>

